export interface Point2D {
    x: number;
    y: number;
}

export interface CanvasDimentions {
    height: number;
    width: number;
}

export interface WindowDimentions {
    height: number;
    width: number;
}